read me

源码没传python embed，需要自己配置python环境

为了实现取消订阅的功能，此软件会占用壁纸引擎的steam appid，所以无法与壁纸引擎同时运行。


godot引擎不支持gif动图显示，所以我只能将动图分割为多张静态图拼接为godot支持的ImageTexture来显示，缺点是静态图片资源会缓存在软件根目录下的gif_cache文件夹内，占用一定磁盘空间，可以点击左上角的 删除gif缓存 按钮来清除缓存。
